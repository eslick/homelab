[defaults]
inventory           = inventory/hosts.ini
roles_path          = roles
retry_files_enabled = False
stdout_callback     = yaml
diff                = True

[privilege_escalation]
become        = False
become_method = sudo
