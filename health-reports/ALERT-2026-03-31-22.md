# Health Report — 2026-03-31 22:00

**Status: ALERT**

---

## 1. Disk Usage

| Mount | Used% | Device |
|-------|-------|--------|
| / | 14% | /dev/mapper/ubuntu--vg-ubuntu--lv |
| /boot | 6% | /dev/nvme0n1p2 |
| /boot/efi | 1% | /dev/nvme0n1p1 |
| /mnt/nas | 39% | //192.168.50.101/ServerBackup |
| /run | 1% | tmpfs |
| /dev/shm | 0% | tmpfs |
| /run/lock | 0% | tmpfs |
| /run/user/1000 | 1% | tmpfs |
| /sys/firmware/efi/efivars | 44% | efivarfs |

**Result: OK** — All mounts below 80% threshold.

---

## 2. Memory Usage

| Metric | Value |
|--------|-------|
| Used | 1.5% |
| Total | ~251 GB |
| Used | ~3.8 GB |
| Free | ~246 GB |

**Result: OK** — Well below 85% threshold.

---

## 3. Docker Containers

No containers found running (`docker ps` returned no rows).

**Result: INFO** — No containers currently deployed. Verify this is expected.

---

## 4. Restic Last Snapshot

```
restic_error: could not connect to repository at /mnt/nas/backups/homelab
```

**Result: ALERT** — Could not verify last backup snapshot. Repository at `/mnt/nas/backups/homelab` is inaccessible. Last snapshot age cannot be confirmed as within the 25-hour threshold. Investigate NAS mount and restic repository path.

---

## 5. Failed Systemd Units

```
  UNIT              LOAD   ACTIVE SUB    DESCRIPTION
● postfix@-.service loaded failed failed Postfix Mail Transport Agent (instance -)
```

**Result: ALERT** — `postfix@-.service` is in a failed state. Investigate with `journalctl -u postfix@-.service`.

---

## 6. Pending Security Updates

No pending security updates found.

**Result: OK**

---

## Summary

| Check | Status |
|-------|--------|
| Disk Usage | OK |
| Memory | OK |
| Docker Containers | INFO |
| Restic Snapshot | **ALERT** |
| Systemd Units | **ALERT** |
| Security Updates | OK |

### Action Required
1. **Restic**: Investigate why `/mnt/nas/backups/homelab` is not accessible. Check that NAS is mounted and restic repo is initialized at the correct path.
2. **Postfix**: `postfix@-.service` has failed. Run `journalctl -u postfix@-.service --no-pager -n 50` to diagnose.
